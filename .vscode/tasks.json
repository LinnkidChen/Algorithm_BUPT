{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cd exp2 && g++ gen_point.cpp -o gen_point&&./gen_point &&g++ -g point.cpp -o point"
        }
    ]
}